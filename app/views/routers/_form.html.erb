<%= form_with(model: router, local: true) do |form| %>
  <% if router.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(router.errors.count, "error") %> prohibited this router from being saved:</h2>

      <ul>
      <% router.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :router_name %>
  </div>

  <div class="field">
    <%= form.label :latitude %>
    <%= form.text_field :latitude, id: :router_latitude %>
  </div>

  <div class="field">
    <%= form.label :longitude %>
    <%= form.text_field :longitude, id: :router_longitude %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
